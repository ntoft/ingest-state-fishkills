{
  "name": "ingest-state-fishkills",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "action": "bun dist/action.js",
    "build": "bun build src/action.ts --target=bun --outfile=dist/action.js",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pdf-parse": "^1.1.4",
    "typescript": "^5.0.0"
  }
}
